# Frontend environment (Vite)
# Copy to .env and adjust values

# Set to "true" to route CRM data through NocoDB instead of the Hono REST API.
# Requires NocoDB to be running and configured (see docker-compose.yml).
VITE_USE_NOCODB=false
